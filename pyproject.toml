[project]
name = "esa"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "esa-exporter",
]

[tool.uv.workspace]
members = [
    "esa_exporter",
]

[tool.uv.sources]
esa-exporter = { workspace = true }
